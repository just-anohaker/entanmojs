"use strict";

/// class Accounts for module [accounts]
const Accounts = require("./accounts");

/// class Blocks for module [blocks]
const Blocks = require("./blocks");

/// class Delegates for module [delegate]
const Delegates = require("./delegates");

/// class Loader for module [loader]
const Loader = require("./loader");

/// class LockVote for module [lockvote]
const LockVote = require("./lockvote");

/// class Multisignatures for module [multisignatures]
const Multisignatures = require("./multisignatures");

/// class Peer for module [peer]
const Peer = require("./peer");

/// class Peer for module [server]
const Server = require("./server");

/// class Signatures for module [signatures]
const Signatures = require("./signatures");

/// class System for module [system]
const System = require("./system");

/// class Transactions for module [transactions]
const Transactions = require("./transactions");

/**
 * @module
 * 
 * Node Modules Classes
 */
module.exports = {
    Accounts,
    Blocks,
    Delegates,
    Loader,
    LockVote,
    Multisignatures,
    Peer,
    Server,
    Signatures,
    System,
    Transactions,
};